#Run date command and store output to shell variables
now="$(date)"
#now is nothing but the vraible name
#We print it and use the variable
#The variable names are case sensitive so now and NOW are different variable names
echo "This is the current date and time: $now"
